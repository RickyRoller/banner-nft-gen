import { TemplateKeys } from 'banner-templates';
import { FC } from 'react';
import styles from './template-selector.module.scss';

interface Props {}

export const TemplateSelector: FC<Props> = ({}) => {
  return <div className={styles.TemplateSelector}></div>;
};
